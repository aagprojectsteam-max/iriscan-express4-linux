# udev packaging

No production udev rule is shipped yet. The device exposes a mass-storage/SCSI interface, so broad permission rules are intentionally avoided until the final access model is proven safe.
